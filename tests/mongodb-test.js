'use strict';

const assert = require('assert');

const MongoDB = require('./../index');

const { MongoDBError } = require('./../mongodb');

describe('MongoDB', function() {

   const mongoDB = new MongoDB({
      
   });

   describe('checkConn', function() {

   });
});