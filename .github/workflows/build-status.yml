name: Build Status

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    uses: janis-commerce/.github/.github/workflows/build-status.yml@master
    with:
      build-types: true
